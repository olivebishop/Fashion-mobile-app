import React from 'react';
import { View, Text } from 'react-native';

const FavoriteScreen: React.FC = () => {
  return (
    <View className='flex-1 justify-center items-center'>
      <Text className=''>Wishlists</Text>
    </View>
  );
};

export default FavoriteScreen;